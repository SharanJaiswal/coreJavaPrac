package kk.collectionsClass;

/**
 * We are talking about Collections w/ 's', not Collection (one of the implemented interface of Collection classes).
 * This is a utility class providing static methods to operate on collection objects, like sort, binarySearch, get, reverse, shuffle, swap, copy, min, max, rotate, unmodifiableCollection etc.
 * Static methods in Collections supports all type of Collection object. It is a utility class. Just like Array and Arrays are different, same way Collection and Collections are different.
 * It has static methods that work on Collection objects, or returns new Collection objects.
 */
public class CollectionsClass {
}
