package kk.linkLists;

/**
 * For add or remove element from the 0th place, its O(N). While for any place beyond 0th place, the operation is split into 2 operation: traverse [get:O(N)], and then add|remove: O(1).
 *
 */
public class linkList {
}
