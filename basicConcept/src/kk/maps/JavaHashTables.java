package kk.maps;

/**
 * Legacy class. We don't use it now. Implements Map. Existed before Collection Framework.
 * Thread-safe. Unlike HashMap, it is synchronized, as well as has synchronized methods, hence slower than HashMap.
 * Cannot store null key or value.
 * Rest all is same as hashMap
 */
public class JavaHashTables {
}
